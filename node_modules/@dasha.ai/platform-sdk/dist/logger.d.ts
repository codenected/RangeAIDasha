import * as winston from "winston";
export { Logger } from "winston";
export interface Options {
    sendErrorReports?: boolean;
    logLevel?: string;
    logger?: winston.Logger;
}
export declare function configure(options: Options): void;
export declare function create(defaultMeta?: Record<string, unknown>): winston.Logger;
export declare function catchErrors<T>(log: winston.Logger, fn: (log: winston.Logger) => T): T;
export declare function catchErrorsAsync<T>(log: winston.Logger, fn: (log: winston.Logger) => Promise<T>): Promise<T>;
