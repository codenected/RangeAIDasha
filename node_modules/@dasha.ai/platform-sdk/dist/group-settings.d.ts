export declare class GroupSettingsManager {
    private readonly _client;
    constructor(options: {
        server: string;
        apiKey: string;
        insecure?: boolean;
    });
    private _postGroup;
    private _deleteGroup;
    private _patchGroup;
    private _getGroups;
    private _findGroup;
    create(name: string, options?: {
        description?: string;
        maxConcurrency?: number;
    }): Promise<void>;
    delete(name: string): Promise<void>;
    update(name: string, options: {
        name?: string;
        description?: string;
        maxConcurrency?: number;
    }): Promise<void>;
    list(): Promise<{
        id: string;
        name: string;
        description?: string;
        maxConcurrency?: number;
        isDefaultGroup: boolean;
    }[]>;
}
