export interface SynthesisOptions {
    configName?: string;
    voiceInfo: {
        speaker: string;
        language: string;
        emotion?: string;
        speed?: number;
        variation?: number;
    };
}
export interface SynthesisRequestDto {
    configName?: string;
    text: string;
    voiceInfo: {
        speaker: string;
        lang: string;
        emotion?: string;
        speed?: number;
        variation?: number;
    };
}
export declare class TtsManager {
    private readonly _client;
    constructor(options: {
        server: string;
        apiKey: string;
        insecure?: boolean;
    });
    private _postSynthesis;
    synthesize(text: string, options: SynthesisOptions): Promise<Uint8Array>;
}
